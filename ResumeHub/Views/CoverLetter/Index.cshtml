<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>AI Cover Letter Generator</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <style>
        :root {
            --primary-color: #4e73df;
            --secondary-color: #2e59d9;
            --accent-color: #1cc88a;
            --dark-color: #2c3e50;
            --light-color: #f8f9fc;
        }
        
        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            line-height: 1.6;
            color: #4a4a4a;
            overflow-x: hidden;
        }

        .cover-letter-section {
            padding: 5rem 0;
            background-color: var(--light-color);
        }

        .cover-letter-card {
            background: white;
            border-radius: 10px;
            box-shadow: 0 0.15rem 1.75rem 0 rgba(58, 59, 69, 0.15);
            padding: 2.5rem;
            margin-bottom: 2rem;
        }

        .input-label {
            font-weight: 600;
            margin-bottom: 0.8rem;
            color: var(--dark-color);
            display: block;
        }

        .textarea-container {
            position: relative;
            margin-bottom: 1.5rem;
        }

        textarea.form-control {
            min-height: 200px;
            padding: 1.2rem;
            border-radius: 8px;
            border: 1px solid #e0e0e0;
            transition: border-color 0.3s;
            font-size: 1rem;
        }

        textarea.form-control:focus {
            border-color: var(--accent-color);
            box-shadow: 0 0 0 0.25rem rgba(28, 200, 138, 0.25);
            outline: none;
        }

        .word-count {
            position: absolute;
            bottom: 10px;
            right: 15px;
            color: #777;
            font-size: 0.9rem;
            background: rgba(255, 255, 255, 0.8);
            padding: 0 5px;
            border-radius: 3px;
        }

        .preview-container {
            background: white;
            border-radius: 10px;
            padding: 2rem;
            margin-top: 2rem;
            display: none;
            box-shadow: 0 0.15rem 1.75rem 0 rgba(58, 59, 69, 0.1);
        }

        .preview-header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-bottom: 1.5rem;
        }

        .preview-content {
            line-height: 1.8;
            font-size: 1.05rem;
            white-space: pre-wrap;
            background: var(--light-color);
            padding: 1.5rem;
            border-radius: 8px;
            border-left: 4px solid var(--primary-color);
        }

        .btn-accent {
            background-color: var(--accent-color);
            border-color: var(--accent-color);
            color: white;
            font-weight: 600;
            padding: 0.8rem 2.5rem;
            font-size: 1.1rem;
        }

        .btn-accent:hover {
            background-color: #17a673;
            border-color: #17a673;
            color: white;
        }

        .ai-badge {
            background: var(--primary-color);
            color: white;
            padding: 5px 15px;
            border-radius: 20px;
            font-size: 0.9rem;
            font-weight: 600;
            display: inline-block;
            margin-bottom: 1rem;
        }

        .step-number {
            background: var(--primary-color);
            color: white;
            width: 36px;
            height: 36px;
            border-radius: 50%;
            display: inline-flex;
            align-items: center;
            justify-content: center;
            font-weight: 700;
            margin-right: 12px;
        }

        .feature-icon {
            font-size: 2.5rem;
            color: var(--primary-color);
            margin-bottom: 1rem;
        }

        media (max-width: 768px) {
            .cover-letter-section {
                padding: 3rem 0;
            }
            
            .cover-letter-card {
                padding: 1.5rem;
            }
        }
    </style>
</head>
<body>
    <!-- Cover Letter Generator Section -->
    <section class="cover-letter-section" id="cover-letter">
        <div class="container">
            <div class="row justify-content-center">
                <div class="col-lg-10">
                    <h2 class="section-title text-center">AI Cover Letter Generator</h2>
                    <p class="text-center text-muted mb-5">Create a professional cover letter tailored to your job application</p>

                    <div class="cover-letter-card">
                        <div class="ai-badge">Powered by Advanced AI</div>
                        <h3 class="mb-4">What To Provide?</h3>
                        <p class="text-muted mb-4">Fill in the fields below to help our AI create the perfect cover letter:</p>
                        
                        <div class="mb-4">
                            <div class="d-flex align-items-center mb-2">
                                <span class="step-number">1</span>
                                <span>Job description and requirements</span>
                            </div>
                            <div class="d-flex align-items-center mb-2">
                                <span class="step-number">2</span>
                                <span>Your resume, skills, and experience</span>
                            </div>
                            <div class="d-flex align-items-center">
                                <span class="step-number">3</span>
                                <span>Any specific details you want to highlight</span>
                            </div>
                        </div>

                        <form id="letterForm">
                            <div class="mb-4">
                                <label class="input-label">Paste Job Description:</label>
                                <div class="textarea-container">
                                    <textarea class="form-control" id="jobDescription" rows="6" placeholder="Paste the job description, requirements, and any details about the position here..."></textarea>
                                    <div class="word-count" id="jobWordCount">0/1000 words</div>
                                </div>
                            </div>
                            
                            <div class="mb-4">
                                <label class="input-label">Paste Your Resume/Experience:</label>
                                <div class="textarea-container">
                                    <textarea class="form-control" id="userResume" rows="6" placeholder="Paste your resume, work experience, skills, and qualifications here... (Optional but recommended for better results)"></textarea>
                                    <div class="word-count" id="resumeWordCount">0/1000 words</div>
                                </div>
                            </div>
                            
                            <div class="d-grid">
                                <button type="button" class="btn btn-accent btn-lg" id="generateBtn">
                                    <i class="fas fa-bolt me-2"></i> GENERATE COVER LETTER
                                </button>
                            </div>
                        </form>
                    </div>

                    <div class="preview-container" id="previewSection">
                        <div class="preview-header">
                            <h3>Your AI-Generated Cover Letter</h3>
                            <button class="btn btn-primary" id="copyBtn">
                                <i class="fas fa-copy me-1"></i> Copy
                            </button>
                        </div>
                        <div class="preview-content" id="letterPreview">
                            <!-- Preview content will be inserted here by JavaScript -->
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </section>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
    <script>
        document.addEventListener('DOMContentLoaded', function() {
            const jobTextarea = document.getElementById('jobDescription');
            const resumeTextarea = document.getElementById('userResume');
            const jobWordCount = document.getElementById('jobWordCount');
            const resumeWordCount = document.getElementById('resumeWordCount');
            const generateBtn = document.getElementById('generateBtn');
            const previewSection = document.getElementById('previewSection');
            const letterPreview = document.getElementById('letterPreview');
            const copyBtn = document.getElementById('copyBtn');
            
            // Update word count for job description
            jobTextarea.addEventListener('input', function() {
                const text = jobTextarea.value.trim();
                const words = text ? text.split(/\s+/).length : 0;
                jobWordCount.textContent = `${words}/1000 words`;
                jobWordCount.style.color = words > 1000 ? '#e74c3c' : '#777';
            });
            
            // Update word count for resume
            resumeTextarea.addEventListener('input', function() {
                const text = resumeTextarea.value.trim();
                const words = text ? text.split(/\s+/).length : 0;
                resumeWordCount.textContent = `${words}/1000 words`;
                resumeWordCount.style.color = words > 1000 ? '#e74c3c' : '#777';
            });
            
            // Generate cover letter
            generateBtn.addEventListener('click', function() {
                const jobContent = jobTextarea.value.trim();
                
                if (!jobContent) {
                    alert('Please paste the job description before generating a cover letter.');
                    return;
                }
                
                const resumeContent = resumeTextarea.value.trim();
                
                // Show loading state
                generateBtn.innerHTML = '<i class="fas fa-spinner fa-spin me-2"></i> Generating...';
                generateBtn.disabled = true;
                
                // Simulate AI processing
                setTimeout(function() {
                    // Extract information from inputs
                    const companyMatch = jobContent.match(/(company|organization|firm)\s+(is\s+)?([\w\s]+)/i);
                    const companyName = companyMatch ? companyMatch[3] : 'your company';
                    
                    const positionMatch = jobContent.match(/(position|role)\s+(of\s+)?([\w\s]+)/i);
                    const positionName = positionMatch ? positionMatch[3] : 'the position';
                    
                    const nameMatch = jobContent.match(/(hiring\s+)?manager\s+(name\s+is\s+)?([\w\s]+)/i);
                    const hiringManagerName = nameMatch ? nameMatch[3] : '[Hiring Manager Name]';
                    
                    // Extract user details from resume
                    const userNameMatch = resumeContent.match(/(name\s*:\s*)([\w\s]+)/i);
                    const userName = userNameMatch ? userNameMatch[2] : '[Your Name]';
                    
                  
                    
                    const userPhoneMatch = resumeContent.match(/(phone\s*:\s*)([\d\s().-]+)/i);
                    const userPhone = userPhoneMatch ? userPhoneMatch[2] : '[Your Phone Number]';
                    
                    // Sample cover letter template
                    const letter = `${userName}
${userPhone} | ${userEmail}
${new Date().toLocaleDateString('en-US', { year: 'numeric', month: 'long', day: 'numeric' })}

${hiringManagerName}
Hiring Manager
${companyName}
[Company Address]

Dear ${hiringManagerName.split(' ')[0] || 'Hiring Manager'},

I am writing to express my enthusiastic interest in the ${positionName} position at ${companyName}. With my extensive experience in [relevant field/industry] and proven track record in [key skill/achievement], I am confident that I possess the skills and qualifications necessary to excel in this role.

In my previous position at [Previous Company], I successfully [mention a key achievement or responsibility that aligns with the job description]. This experience has equipped me with [mention specific skills or knowledge relevant to the position].

I was particularly drawn to this opportunity at ${companyName} because of [mention something specific about the company]. I am impressed by [specific aspect of the company] and believe my background in [your relevant experience] aligns well with your needs.

I would welcome the opportunity to discuss how my qualifications align with your needs in an interview. Thank you for considering my application.

Sincerely,
${userName}`;
                    
                    letterPreview.textContent = letter;
                    previewSection.style.display = 'block';
                    
                    // Reset button
                    generateBtn.innerHTML = '<i class="fas fa-bolt me-2"></i> GENERATE COVER LETTER';
                    generateBtn.disabled = false;
                    
                    // Scroll to preview
                    previewSection.scrollIntoView({ behavior: 'smooth' });
                }, 2000);
            });
            
            // Copy to clipboard
            copyBtn.addEventListener('click', function() {
                const textToCopy = letterPreview.textContent;
                
                navigator.clipboard.writeText(textToCopy).then(() => {
                    // Show confirmation
                    const originalText = copyBtn.innerHTML;
                    copyBtn.innerHTML = '<i class="fas fa-check me-1"></i> Copied!';
                    
                    setTimeout(() => {
                        copyBtn.innerHTML = originalText;
                    }, 2000);
                }).catch(err => {
                    console.error('Failed to copy: ', err);
                    alert('Failed to copy text to clipboard.');
                });
            });
        });
    </script>
</body>
</html>